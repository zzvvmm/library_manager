<h1>Users</h1>

<% if @users %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <h3>
          <%= link_to user.email, "#" %>
        </h3>
        <p><%= user.name %></p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Your search did not match any documents.</p>
<% end %>
